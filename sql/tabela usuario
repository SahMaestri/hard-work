CREATE TABLE `speed veiculos`.`usuario` (
  `id` INT NOT NULL,
  `nome` VARCHAR(45) NOT NULL,
  `sexo` VARCHAR(1) NOT NULL,
  `login` VARCHAR(45) NOT NULL,
  `senha` INT NOT NULL AUTO_INCREMENT,
  PRIMARY KEY (`id`, `nome`, `sexo`, `login`, `senha`));